@model Navento.Models.Pedido

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Pedido</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Data" class="control-label"></label>
                <input asp-for="Data" class="form-control" />
                <span asp-validation-for="Data" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="UsuarioId" class="control-label"></label>
                <select asp-for="UsuarioId" class ="form-control" asp-items="ViewBag.UsuarioId"></select>
            </div>
            
            @Html.DropDownList("produtos", (MultiSelectList) ViewBag.Produtos, new { multiple = "true" })

            @*<div class="form-group">
                <label asp-for="@Model.PedidosItems.FirstOrDefault().ProdutoId" class="control-label">Produto</label>
                <select asp-for="PedidosItems" class ="form-control" asp-items="ViewBag.Produtos"></select>
            </div>*@
            <div class="form-group">
                <input type="submit" value="Comprar" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
